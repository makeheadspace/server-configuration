pre-start script

bash << "EOF"
  mkdir -p /home/discourse/logs
  chown -R discourse /home/discourse/logs
EOF

end script

start on runlevel [2345]
stop on runlevel [016]
respawn

exec su - discourse -c 'cd /home/discourse/application && foreman start > /home/discourse/logs/log 2>&1'
